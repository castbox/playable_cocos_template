import { _decorator } from "cc";
import { PlayableCamera } from "./playable.camera"
const { ccclass, property } = _decorator;

@ccclass('PlayableCameraPerspective')
export class PlayableCameraPerspective extends PlayableCamera
{
    
}